# =============================================================================
#  pnpm 配置
#
#  ⚠️ 本机（Windows + 杀软实时扫描）下 pnpm 会间歇性报
#     「拒绝访问 (os error 5)」/「系统找不到指定的文件 (os error 2)」——
#     与 cargo 编译时遇到的是**同一个**并发写盘问题。
#
#  实测结论：
#  1. **store-dir 放在盘根最不稳**（pnpm 默认 `<盘符>:\.pnpm-store`），
#     放到工作区内明显改善
#  2. **`pnpm config get store-dir` 不读项目级 .npmrc**，
#     因此装依赖时请**显式传参**（见下方命令），不要指望本文件生效
#  3. 并发降到 1 后能稳定下包（代价是慢，约 15 分钟）
#
#  ✅ 可复现的安装命令（推荐写进 CI 或交给新人）：
#
#     pnpm install \
#       --store-dir "D:/workbuddy-ai_Work/.pnpm-store" \
#       --network-concurrency 1 \
#       --child-concurrency 1
#
#  安装后如报 "Failed to create symlink ... os error 2"，
#  多半是上一次失败留下的不完整包 —— **再跑一次同样的命令**通常即可。
#  （已确认软链接最终是正确创建的，pnpm 报的可能是重试残留。）
#
#  构建时若 pnpm 又自动触发 install 并失败，可直接绕过：
#     ./node_modules/.bin/vite build
#     ./node_modules/.bin/vue-tsc --noEmit
#
#  ⚠️ `pnpm add` 的注意事项（2026-09-17 实测）
#
#  1. **`pnpm add` 不认 `--network-concurrency`**（只有 `install` 认），
#     传了会直接报 `unexpected argument`。`add` 时并发参数从本文件读即可。
#  2. **`pnpm add` 可能「包已装好但 package.json 没更新」**：
#     命令以 `拒绝访问` 报错退出、脚本重试 5 次全失败，
#     但 `node_modules/<pkg>` 其实已经完整落地且可 `require`。
#     → 此时**手工把依赖写进 package.json 即可**，不要重复装。
#     判断方法：`node -e "require('<pkg>')"` 能跑通就说明包是好的。
#
#  换机时删掉本文件即可恢复 pnpm 默认行为。
# =============================================================================

store-dir=D:/workbuddy-ai_Work/.pnpm-store

# ⚠️ 上面这条是**开发机专用**的绝对路径。CI（GitHub Actions runner）上没有这个盘，
#    所以 `.github/workflows/ci.yml` 里**显式传 `--store-dir "${{ runner.temp }}/pnpm-store"`**
#    覆盖它。改这里时记得两边一起看。

# 降低并发：同时写的文件越少，越不容易撞上写入被拒
network-concurrency=1
child-concurrency=1

# ⚠️ 关掉「执行脚本前自动校验/安装依赖」（pnpm 9+ 的 verify-deps-before-run）。
#
# 原因（2026-09-18 实测）：本机创建软链接会间歇性失败
# （`Failed to create symlink ... os error 2`，杀软实时扫描持句柄）。
# 一旦 `node_modules` 与锁文件的对账结果被判为「不一致」，
# `pnpm build` 就会**先尝试装依赖再跑脚本** —— 于是构建被一个与构建无关的
# 链接问题卡死（每次失败在不同的包上，重试无解）。
#
# 关掉后 `pnpm build` 直接跑脚本。构建真正需要的包本来就在
# `node_modules/` 里能解析（`vite build` / `vue-tsc` 单独跑都正常）。
#
# ⚠️ CI 不受影响：那边先做一次干净的 `pnpm install --frozen-lockfile`，
#    本项只管「跑脚本前的那次额外对账」。
verify-deps-before-run=false

# 国内镜像（与全局 ~/.npmrc 一致，显式写出避免依赖全局配置）
registry=https://registry.npmmirror.com
